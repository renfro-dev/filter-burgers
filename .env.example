
## `package.json`
```json
{
  "name": "filter-burgers",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev:function": "supabase functions serve --no-verify-jwt ingest_url",
    "dev:stack": "supabase start",
    "test": "vitest --run",
    "build": "echo 'Edge Functions use Deno (no build step). TS used for local tooling/tests.'"
  },
  "devDependencies": {
    "@types/jsdom": "^21.1.7",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "jsdom": "^24.1.0",
    "node-fetch": "^3.3.2",
    "readability-node": "^0.0.5"
  }
}
